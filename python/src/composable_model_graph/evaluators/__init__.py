"""Evaluators (Python port pending parity).

The TypeScript package ships threshold / numeric-error / exact-match / composite
evaluators (see ../../../typescript/packages/evaluators). They are ported here in
a follow-up feature PR, with parity to TypeScript, because every capability ships
in BOTH languages (see docs/development.md). This module is an intentional
placeholder until then.
"""

__all__: list[str] = []
