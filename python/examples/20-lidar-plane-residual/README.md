# 20 - Find where two runs diverge

Python parity for
[`typescript/examples/20-lidar-plane-residual/README.md`](../../../typescript/examples/20-lidar-plane-residual/README.md),
which has the full walkthrough. No robotics background needed.

Run:

```sh
python3 python/examples/20-lidar-plane-residual/main.py
```

Produces the same output as the TypeScript version (see
[`expected-output.txt`](expected-output.txt)).
